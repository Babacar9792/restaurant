import { IntituleCommandePipe } from './intitule-commande.pipe';

describe('IntituleCommandePipe', () => {
  it('create an instance', () => {
    const pipe = new IntituleCommandePipe();
    expect(pipe).toBeTruthy();
  });
});
